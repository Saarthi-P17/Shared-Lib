// Jenkinsfile
// security-lib is the name of the authrized shared library defined in Jenkins
@Library('security-lib') _

securityPipeline(
    reportDir: 'reports',
    // approvedAmi: 'ami-1234567890abcdef',
    slackChannel: '#ci-operation-notifications'
)